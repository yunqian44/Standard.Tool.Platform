<Window x:Class="Standard.Tool.Platform.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Standard.Tool.Platform" 
        x:Name="WindowMain"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        MouseLeftButtonDown="WindowMain_MouseLeftButtonDown"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Height="700"
        Width="1180"
        Title="MainWindow">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="70"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Border CornerRadius="10 10 0 0">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="0.5,0.5">
                    <GradientStop Color="#E7E9EB" Offset="0"/>
                    <GradientStop Color="#E7E9EB" Offset="1"/>
                </LinearGradientBrush>
            </Border.Background>
            <Border Grid.Row="0" x:Name="BorderTitleBar" Padding="4">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                        <Menu VerticalAlignment="Center" FontSize="14">
                            <MenuItem Header="{DynamicResource Mnu_System}">
                                <MenuItem Header="{DynamicResource Mni_About}"
                                  Command="{Binding AboutApp}">
                                    <MenuItem.Icon>
                                        <StackPanel>
                                            <Image Source="/eBRClient;component/Resources/Images/Icons/about.png"/>
                                        </StackPanel>
                                    </MenuItem.Icon>
                                </MenuItem>
                                <Separator/>
                                <MenuItem Header="{DynamicResource Mni_LogIn}"
                                  Command="{Binding LogIn}" InputGestureText="Ctrl+L">
                                    <MenuItem.Icon>
                                        <StackPanel>
                                            <Image Source="/eBRClient;component/Resources/Images/Icons/login.png"/>
                                        </StackPanel>
                                    </MenuItem.Icon>
                                </MenuItem>
                                <MenuItem Header="{DynamicResource Mni_LogOut}"
                                  Command="{Binding LogOut}" InputGestureText="Esc">
                                    <MenuItem.Icon>
                                        <StackPanel>
                                            <Image Source="/eBRClient;component/Resources/Images/Icons/logout.png"/>
                                        </StackPanel>
                                    </MenuItem.Icon>
                                </MenuItem>
                                <Separator/>
                                <MenuItem Header="{DynamicResource Mni_Exit}"
                                  Command="{Binding Exit}"
                                  CommandParameter="{Binding ElementName=WindowMain}" InputGestureText="Alt+X">
                                    <MenuItem.Icon>
                                        <StackPanel>
                                            <Image Source="/eBRClient;component/Resources/Images/Icons/exit.png"/>
                                        </StackPanel>
                                    </MenuItem.Icon>
                                </MenuItem>
                            </MenuItem>
                        </Menu>
                        <Image Source="Image/siemens.png"/>
                        <Label Content="eBR Package Tools" VerticalAlignment="Center" FontWeight="Bold" FontSize="28"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Border>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="250"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Border CornerRadius="0 0 0 10" Grid.Column="0">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="0.5,0.5">
                        <GradientStop Color="#E7E9EB" Offset="0"/>
                        <GradientStop Color="#E7E9EB" Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>
            </Border>
            <Border CornerRadius="0 0 10 0" Grid.Column="1">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                        <GradientStop Color="#DCDCDC" Offset="0" />
                        <GradientStop Color="#F5F5F5" Offset="1" />
                    </LinearGradientBrush>
                </Border.Background>
            </Border>
            <Grid Grid.Column="0">
                <!--<Grid.RowDefinitions>
                    <RowDefinition Height="70"/>
                    <RowDefinition Height="326*"/>
                </Grid.RowDefinitions>
                <Border Grid.Row="0" Background="#FFFFFF">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="23*"/>
                            <ColumnDefinition Width="227*"/>
                        </Grid.ColumnDefinitions>
                        <Image Source="Image/siemens.png" Grid.ColumnSpan="2"/>
                    </Grid>
                </Border>-->

                <ScrollViewer HorizontalAlignment="Stretch" HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto" >
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="80"/>
                        </Grid.RowDefinitions>
                        <StackPanel Grid.Row="0" x:Name="Menu" Margin="10"/>
                        <StackPanel Grid.Row="1">
                            <Separator  Background="#e0e0e0" Opacity="1" Margin="50 0 60 0"/>

                            <!--<Button Style="{StaticResource menuMainButton}">
                                <Grid>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                                        <materialDesign:PackIcon Kind="FolderOutline" Style="{StaticResource buttonIcon}" />
                                        <TextBlock Text="账号" Style="{StaticResource buttonText}" />
                                    </StackPanel>
                                </Grid>
                            </Button>-->

                            <Button Style="{StaticResource menuMainButton}" Command="{Binding SignOut}"  CommandParameter="{Binding ElementName=WindowMain}">
                                <Grid>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                                        <materialDesign:PackIcon Kind="CircleOutline" Style="{StaticResource buttonIcon}" />
                                        <TextBlock Text="退出" Style="{StaticResource buttonText}" />
                                    </StackPanel>
                                </Grid>
                            </Button>
                        </StackPanel>
                    </Grid>
                </ScrollViewer>

            </Grid>
            <Border Grid.Column="2">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Border Grid.Row="0" Background="#F5F5F5">
                        <StackPanel  Orientation="Horizontal" HorizontalAlignment="Left"  Margin="5">
                            <TextBlock Text="{DynamicResource Tit_MainWindow}" FontSize="18" VerticalAlignment="Center"/>
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <TextBlock Text="{Binding PageName}" VerticalAlignment="Center" Margin="4" FontWeight="Black"/>
                                <TextBlock Text="{Binding PageVersion}" VerticalAlignment="Center" Margin="4"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                    <Border Grid.Row="1" Background="#F5F5F5">
                        <Frame x:Name="StackPanelMain" HorizontalAlignment="Stretch" NavigationUIVisibility="Hidden" Content="{Binding CurrentPage}"/>
                    </Border>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>
