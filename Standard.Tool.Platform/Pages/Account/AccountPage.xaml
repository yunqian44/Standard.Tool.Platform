<Page x:Class="Standard.Tool.Platform.Pages.Account.AccountPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Standard.Tool.Platform.Pages.Account"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      mc:Ignorable="d"
      Title="用户管理">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#F5F5F5">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="80"/>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="80"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="80"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions >

                <StackPanel Margin="10" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <Label Content="用户组" />
                </StackPanel>

                <StackPanel Grid.Column="1" Margin="10" HorizontalAlignment="Left">
                    <TextBox Text="请选择" Width="120" BorderBrush="#009999" BorderThickness="1"/>
                </StackPanel>


                <StackPanel Grid.Column="2" Margin="10" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <Label Content="状态" />
                </StackPanel>

                <StackPanel Grid.Column="3" Margin="10" HorizontalAlignment="Left">
                    <TextBox Text="请选择" Width="120" BorderBrush="#009999" BorderThickness="1"/>
                </StackPanel>

                <StackPanel Grid.Column="4" Margin="10" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <Label Content="全名" />
                </StackPanel>

                <StackPanel Grid.Column="5" Margin="10" HorizontalAlignment="Left">
                    <TextBox Text="请选择" Width="120" BorderBrush="#009999" BorderThickness="1"/>
                </StackPanel>


                <StackPanel Grid.Row="1" Grid.Column="0" Margin="10" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <Label Content="名称" />
                </StackPanel>

                <StackPanel Grid.Row="1" Grid.Column="1" Margin="10" HorizontalAlignment="Left" VerticalAlignment="Center">
                    <TextBox Text="请选择" Width="120" BorderBrush="#009999" BorderThickness="1"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right"  Grid.Row="1" Grid.Column="2" Grid.ColumnSpan="4" >
                    <Button Style="{StaticResource operationButton}" Command="{Binding Refresh}">
                        <StackPanel Orientation="Horizontal"  HorizontalAlignment="Center" VerticalAlignment="Center" >
                            <materialDesign:PackIcon Kind="CircleOutline" Style="{StaticResource buttonIcon}" />
                            <Label Content="刷新" VerticalAlignment="Center" Margin="5 0 0 0" Foreground="#FFFFFF"/>
                        </StackPanel>
                    </Button>
                    <Button Style="{StaticResource operationButton}" Command="{Binding Search}">
                        <StackPanel Orientation="Horizontal"  HorizontalAlignment="Center" VerticalAlignment="Center" >
                            <materialDesign:PackIcon Kind="Magnify" Style="{StaticResource buttonIcon}" />
                            <Label Content="搜索" VerticalAlignment="Center" Margin="5 0 0 0" Foreground="#FFFFFF"/>
                        </StackPanel>
                    </Button>
                </StackPanel>

            </Grid>
        </Border>

        <Border Grid.Row="1" Background="#F5F5F5">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Orientation="Horizontal"  Margin="5" >
                    <Button Style="{StaticResource operationButton}" Command="{Binding Refresh}">
                        <StackPanel Orientation="Horizontal"  HorizontalAlignment="Center" VerticalAlignment="Center" >
                            <Label Content="导出至目标库" VerticalAlignment="Center" Margin="5 0 0 0" Foreground="#FFFFFF"/>
                        </StackPanel>
                    </Button>
                    <Button Style="{StaticResource operationButton}" Command="{Binding Refresh}">
                        <StackPanel Orientation="Horizontal"  HorizontalAlignment="Center" VerticalAlignment="Center" >
                            <Label Content="导出至本地" VerticalAlignment="Center" Margin="5 0 0 0" Foreground="#FFFFFF"/>
                        </StackPanel>
                    </Button>
                </StackPanel>

                <Grid Grid.Row="1"  Margin="5">
                    <DataGrid IsReadOnly="True" AutoGenerateColumns="False">
                        <DataGrid.Columns>
                            <DataGridCheckBoxColumn CanUserResize="False" Width="*">
                                <DataGridCheckBoxColumn.HeaderTemplate>
                                    <DataTemplate>
                                        <CheckBox/>
                                    </DataTemplate>
                                </DataGridCheckBoxColumn.HeaderTemplate>
                            </DataGridCheckBoxColumn>
                            <DataGridTextColumn Header="序号" Width="*" />
                            <DataGridTextColumn Header="名称" Width="*" />
                            <DataGridTextColumn Header="缩写" Width="*" />
                            <DataGridTextColumn Header="有效期"  Width="*" />
                            <DataGridTextColumn Header="状态" Width="*" />
                            <DataGridTemplateColumn Header="操作" IsReadOnly="True" Width="*">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <Button Width="80" Content="编辑" />
                                            <Button Width="80" Content="查看" />
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>

                <Border Grid.Row="2" BorderBrush="#DEE4EC" BorderThickness="1" CornerRadius="5" Padding="5" HorizontalAlignment="Right" Background="#FFFFFF" Margin="0 10 0 10">
                    <StackPanel Orientation="Horizontal">
                        <Button Style="{StaticResource pagingButton}">
                            <materialDesign:PackIcon Kind="PageFirst" Style="{StaticResource pagingButtonIcon}"/>
                        </Button>
                        <Rectangle Width="1" Fill="#DEE4EC" Margin="3 7"/>
                        <Button Style="{StaticResource pagingButton}" Content="1"/>
                        <Rectangle Width="1" Fill="#DEE4EC" Margin="3 7"/>
                        <Button Style="{StaticResource pagingButton}"  Content="2"/>
                        <Rectangle Width="1" Fill="#DEE4EC" Margin="3 7"/>
                        <Button Style="{StaticResource pagingButton}" Content="..." />
                        <Rectangle Width="1" Fill="#DEE4EC" Margin="3 7"/>
                        <Button Style="{StaticResource pagingButton}"  Content="7"/>
                        <Rectangle Width="1" Fill="#DEE4EC" Margin="3 7"/>
                        <Button Style="{StaticResource pagingButton}" Content="8" />
                        <Rectangle Width="1" Fill="#DEE4EC" Margin="3 7"/>
                        <Button Style="{StaticResource pagingButton}">
                            <materialDesign:PackIcon Kind="PageLast" Style="{StaticResource pagingButtonIcon}"/>
                        </Button>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>
    </Grid>
</Page>
